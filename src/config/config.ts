// eslint-disable-next-line @typescript-eslint/no-var-requires
// const version = require('!!raw-loader!./version.txt');
import version from '!!raw-loader!./version.txt';

export default {
	version,
};
